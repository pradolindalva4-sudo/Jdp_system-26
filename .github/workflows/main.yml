name: Build Jdp_system-26
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: JDp_system-26
          path: .
